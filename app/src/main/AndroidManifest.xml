<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quiz2">

        <!-- Splash Screen — launches first -->
        <activity android:name=".SplashActivity" android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main form screen -->
        <activity android:name=".MainActivity" android:exported="false"
            android:label="Register Complaint"/>

        <!-- Complaint list screen -->
        <activity android:name=".ComplaintListActivity" android:exported="false"
            android:label="Complaints" android:parentActivityName=".MainActivity"/>

        <!-- Detail screen -->
        <activity android:name=".DetailActivity" android:exported="false"
            android:label="Complaint Detail" android:parentActivityName=".ComplaintListActivity"/>

    </application>

</manifest>